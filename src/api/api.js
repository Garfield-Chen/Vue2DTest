import fetch from '@/api/fetch.js'
import config from '@/assets/config/index'

const api_game = {
  //同步卡包
  // Synchronous: data => {
  //   return fetch.post(`${config.baseUrl}/api/package/synchronizeCardPackage`,data)
  // },
}

 

export { api_game }
